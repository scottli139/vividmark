[package]
name = "app"
version = "0.8.3"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.4", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.10.0", features = ["protocol-asset", "image-png"] }
tauri-plugin-log = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
tauri-plugin-clipboard-manager = "2"
notify = "6.1"
tokio = { version = "1", features = ["sync", "time"] }

# Linux：deepin 主题下注入 GTK CSS 修正菜单弹窗文字垂直偏移；摘除菜单栏
# 项空图标占位修正水平偏移（均见 lib.rs）
[target.'cfg(target_os = "linux")'.dependencies]
gtk = "0.18"
gdk = "0.18"

# macOS Dock 右键菜单（applicationDockMenu:）——版本与 tao 0.34 对齐，避免双主版本
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", features = [
    "NSApplication",
    "NSMenu",
    "NSMenuItem",
    "NSPrintInfo",
    "NSPrintOperation",
    "NSView",
    "NSWindow",
] }
objc2-foundation = { version = "0.3", features = [
    "NSObject",
    "NSString",
    "NSURL",
    "NSDictionary",
    "NSArray",
] }
objc2-web-kit = { version = "0.3", features = ["WKWebView", "objc2-app-kit"] }
objc2-pdf-kit = { version = "0.3", features = [
    "PDFDocument",
    "PDFOutline",
    "PDFDestination",
    "PDFSelection",
    "PDFPage",
] }
unicode-normalization = "0.1"

# Windows PDF 直存（WebView2 ICoreWebView2_7::PrintToPdf）——版本与 wry 0.54 对齐
[target.'cfg(windows)'.dependencies]
webview2-com = "0.38"
windows = "0.61"
