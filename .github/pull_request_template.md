## Summary / 概述

<!-- What does this PR do? Link the related issue if any. / 这个 PR 做了什么？关联 issue 请链接。 -->

Closes #

## Type / 类型

- [ ] Bug fix / 修复
- [ ] New feature / 新功能
- [ ] Refactor / 重构
- [ ] Docs / 文档
- [ ] Tests / 测试
- [ ] Chore / 工程化

## Checklist / 检查清单

- [ ] `pnpm tsc -b` passes（必须是 `tsc -b`，不是 `tsc --noEmit`）
- [ ] `pnpm lint` and `pnpm format:check` pass
- [ ] `pnpm test:run` passes; new logic has tests / 新逻辑已补测试
- [ ] UI strings added to both `en.json` and `zh-CN.json` / UI 文案已双语
- [ ] Docs updated in pairs if user-facing (README ↔ README.zh-CN, docs/index ↔ index.zh-CN) / 用户可见变更已双语同步文档
- [ ] New syntax capability comes with an `examples/` file / 新语法已在 examples/ 加示例
